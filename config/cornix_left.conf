# スリープ系の機能をすべてオフにする（バッテリー持ち度外視）
CONFIG_ZMK_SLEEP=n
CONFIG_ZMK_IDLE_TIMEOUT=86400000

# Bluetoothの送信出力を最大（+8dBm）にする
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
# （オプション）ZMKのBluetooth接続安定化の実験的機能を有効にする
CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES=y

# ZMK Studioのアンロックキー操作を不要にする
CONFIG_ZMK_STUDIO_LOCKING=n
